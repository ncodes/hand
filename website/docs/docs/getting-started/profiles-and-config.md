---
title: Profiles and Config
description: Understand profile homes, active profiles, and config precedence.
---

# Profiles and Config

Hand keeps local configuration and state under profiles.

## Content Outline

- What a profile is.
- Default profile location.
- `hand profile init`, `use`, `current`, `path`, and `doctor`.
- `hand config get` and `hand config set`.
- Config precedence: flags, environment, and profile config.
- When changing profiles affects a running daemon.
